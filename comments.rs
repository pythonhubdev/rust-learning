/// This is a documentation comment in Rust.
/// These are called Outer Doc Comments in Rust. This will generate documentation for the following item
/// The Outer Doc Comments supports markdown notation
fn main() {
    // A single line comment in Rust starts with two slashes

    /*
       A multi-line comment in Rust starts with a forward slash followed by an asterisk
       and ends with an asterisk followed by a forward slash
    */

    //! These are called Inner Doc Comments in Rust. This will generate documentation for the enclosing item
}
