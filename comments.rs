fn main() {
    // A single line comment in Rust starts with two slashes

    /*
        A multi-line comment in Rust starts with a forward slash followed by an asterisk
        and ends with an asterisk followed by a forward slash
     */
}